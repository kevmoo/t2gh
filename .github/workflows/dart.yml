name: Dart CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  analyze:
    strategy:
      matrix:
        dart: [dev, latest, beta, '2.9']
    runs-on: ubuntu-latest
    container:
      image:  google/dart:${{ matrix.dart }}
    steps:
    - uses: actions/checkout@v2
    - run: pub get
    - run: dartanalyzer --fatal-warnings --fatal-infos .

  dartfmt:
    runs-on: ubuntu-latest
    container:
      image:  google/dart:latest
    steps:
    - uses: actions/checkout@v2
    - run: dartfmt -n .

  test:
    runs-on: ubuntu-latest
    container:
      image:  google/dart:latest
    steps:
    - uses: actions/checkout@v2
    - run: pub get
    - run: pub run test
